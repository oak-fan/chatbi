# 错误点与分歧点检查清单

## 第1步：SELECT列

**（1）业务口径**

- example数据库中，问题中的"活跃用户"，是指`user_table.login_count > 0`还是`user_table.order_count > 0`；
- california_schools数据库中，问题中的"特许学校"，是指`Charter School (Y/N) = 1`（标志位判断）还是`FundingType = 'Directly funded'`（资助类型，仅部分特许学校）；
- california_schools数据库中，问题中的"Continuation School（续办学校）"，是指`Educational Option Type = 'Continuation School'`（教育选项类型）还是`School Type = 'Continuation Schools (Public)'`（学校类型分类）；
- california_schools数据库中，问题中的"Fresno Unified"，是指`District = 'Fresno Unified'`（学区名称）还是`County = 'Fresno'`（县名）/`City = 'Fresno'`（城市名）；
- california_schools数据库中，问题中的"高中水平学校(high school level)"，是指`EILCode = 'HS'`（教育机构级别代码）还是`GSserved = '9-12'`（提供的年级范围）；
- california_schools数据库中，问题中的"State Special Schools（州特殊学校）"，是指`DOC = 31`（学校分类代码）还是`StatusType = 'Special'`（状态类型字段）；
- california_schools数据库中，问题中的"幼儿园到8年级(Kindergarten to 8th grade)"，是指`GSoffered = 'K-8'`（开设的年级范围）还是`GSserved = 'K-8'`（服务的年级范围，两者语义不同）；
- financial数据库中，问题中的"发行后交易(issuance after transaction)"，是指`account.frequency = 'POPLATEK PO OBRATU'`（账户的账单发行频率）还是`trans.type = 'POPLATEK PO OBRATU'`（交易类型，表名和字段均不同）；
- financial数据库中，问题中的"男性客户"，是指`client.gender = 'M'`（单字符编码）还是`client.sex = 'Male'`（全称字段）；
- toxicology数据库中，问题中的"含钠原子的分子(molecule containing sodium atoms)"，是指`atom.element = 'na'`（化学元素符号na）还是`molecule.has_sodium = 1`（布尔标志位）；
- card_games数据库中，问题中的"极为强大的闪卡(incredibly powerful foils)"，是指`cardKingdomFoilId IS NOT NULL AND cardKingdomId IS NOT NULL`（闪卡ID和普通ID均非空）还是`hasFoil = 1`（简单布尔标志）；
- card_games数据库中，问题中的"无边框卡牌(borderless cards)"，是指`borderColor = 'borderless'`（边框颜色字段）还是`frameEffects = 'extendedart'`（框架效果字段）；
- example数据库中，问题中的"销售额"，是指`order_table.amount`（实付）还是`order_table.total_amount`（总价）；
- california_schools数据库中，问题中的"学校名称"，是指`T2.School`（学校全名）还是`T1.CDSCode`（学校唯一编码，通常也可标识学校）；
- thrombosis_prediction数据库中，问题中的"患者症状"，是指`T2.Symptoms`（具体症状描述）还是`T1.Diagnosis`（诊断结果，两者语义不同）；
- financial数据库中，问题中的"金额"，是指`amount`（交易金额）还是`price`（单价，二者通常不相等）；
- example数据库中，问题中的"已发货"，是指`order_table.delivery_status = 'shipped'`（状态字段）还是`order_table.logistics_no IS NOT NULL`（物流单号非空）；
- california_schools数据库中，问题中的"已关闭学校"，是指`ClosedDate IS NOT NULL`（有关闭日期的）还是`Status = 'Closed'`（状态标记），需确认业务含义；



**（2）去重语义**

- toxicology数据库中，问题中的"列出所有元素"，是指`SELECT DISTINCT element`（去重，一元素一行）还是`SELECT element`（返回所有行，含重复）；




**（3）是否需要计算字段**

- example数据库中，问题中的"同比增速"，是用`product_table.growth_rate`（已有）还是`(product_table.current - product_table.prior)/product_table.prior`（需计算）；
- california_schools数据库中，问题中的"符合条件的免费餐比例(eligible free rate)"，是用`Free Meal Count (Ages 5-17) / Enrollment (Ages 5-17)`（需计算）还是`Percent (%) Eligible Free (K-12)`（已有百分比字段，注意年级范围可能不同）；
- california_schools数据库中，问题中的"FRPM比例(eligible free or reduced price meal rate)"，是用`FRPM Count (K-12) / Enrollment (K-12)`（需计算）还是`Percent (%) Eligible FRPM (K-12)`（已有预存百分比字段，两者数值相同但来源不同）；
- california_schools数据库中，问题中的"各科平均分(average score for all subjects)"，是用`AvgScrMath + AvgScrRead + AvgScrWrite`（三科求和）还是`(AvgScrMath + AvgScrRead + AvgScrWrite) / 3`（求平均值），需确认问题要求的是总和还是均值；
- california_schools数据库中，问题中的"K-12入学人数与5-17岁入学人数之差"，是用`Enrollment (K-12) - Enrollment (Ages 5-17)`（需计算）还是直接取已有差值字段（如有）；
- california_schools数据库中，问题中的"平均数学平均分(average of average math score)"，是用`SUM(AvgScrMath) / COUNT(schools)`（二次聚合计算）还是直接取`AvgScrMath`（单次平均值，语义不同）；
- financial数据库中，问题中的"失业率增量百分比(unemployment increment rate)"，是用`(A13 - A12) / A12 * 100`（需跨年计算）还是直接取`A13`（仅1996年失业率，未计算增量）；
- financial数据库中，问题中的"贷款增长率(growth rate of loan amount)"，是用`(sum_1997 - sum_1996) / sum_1996 * 100`（需跨年聚合计算）还是直接取已有增长率字段；
- financial数据库中，问题中的"最高平均工资与最低平均工资之差"，是用`MAX(A11) - MIN(A11)`（需计算）还是直接取`A11`（仅平均工资字段）；
- financial数据库中，问题中的"男性客户百分比"，是用`COUNT(gender = 'M') * 100 / COUNT(*)`（需计算）还是直接取已有比例字段；
- toxicology数据库中，问题中的"双键百分比(percentage of double bonds)"，是用`COUNT(bond_type = '=') * 100 / COUNT(bond_id)`（需计算）还是直接取已有百分比字段（需确认是否存在）；
- toxicology数据库中，问题中的"致癌分子比例(percentage of carcinogenic molecules)"，是用`COUNT(label = '+') * 100 / COUNT(molecule_id)`（需计算）还是直接取已有比例字段；
- toxicology数据库中，问题中的"单键分子中氧原子的平均数(average number of oxygen atoms)"，是用子查询先按分子聚合`COUNT(oxygen)`再`AVG()`（两步计算）还是直接在原子级别`AVG(CASE WHEN element = 'o' THEN 1 ELSE 0 END)`（一步聚合，结果不同）；
- card_games数据库中，问题中的"西班牙语卡牌百分比(percentage of cards in Spanish)"，是用`COUNT(language = 'Spanish') * 100 / COUNT(*)`（需计算）还是直接取已有百分比字段；
- card_games数据库中，问题中的"有文本框的卡牌比例(proportion of cards with text box)"，是用`COUNT(isTextless = 0) * 100 / COUNT(*)`（需计算）还是直接取已有比例字段；
- thrombosis_prediction数据库中，问题中的"正常范围内的尿酸值"，是用`UA > 8.0 AND SEX = 'M' OR UA > 6.5 AND SEX = 'F'`（需分性别判断）还是统一用`UA BETWEEN 3.5 AND 7.2`（忽略性别差异）；




## 第2步：FROM表选择

**（1）同义/相似表**

- example数据库中，问题中的"订单"，是指`order_master`（主表，一订单一行）还是`order_detail`（明细表，一订单多行）；




## 第3步：JOIN关联

**（1）JOIN类型**

- example数据库中，问题中的"每个员工及其部门信息"，是用`employee_table LEFT JOIN department_table`（保留无部门员工）还是`INNER JOIN`（只保留有部门员工）；
- california_schools数据库中，问题中的"所有学校及其考试分数"，是指`LEFT JOIN satscores`（保留无分数学校）还是`INNER JOIN satscores`（仅保留有分数学校）；
- toxicology数据库中，问题中的"分子中的元素"，是通过`atom.molecule_id = bond.molecule_id`直接关联原子与化学键，还是通过`atom.atom_id = connected.atom_id`再经`connected.bond_id = bond.bond_id`间接关联；
- codebase_community数据库中，问题中的"帖子的编辑者"，是指用`posts.OwnerUserId = users.Id`（帖子作者）还是`posts.LastEditorUserId = users.Id`（最后编辑者）；
- financial数据库中，问题中的"客户及其交易"，是指`INNER JOIN trans`（仅保留有交易记录的客户）还是`LEFT JOIN trans`（保留所有客户，即使无交易）；
- thrombosis_prediction数据库中，问题中的"患者的检验结果"，是指通过`Patient.ID = Laboratory.ID`直接关联，还是通过`Patient.PatientID = Laboratory.PatientID`关联（字段名不同）；



**（2）关联条件中的额外过滤**

- example数据库中，关联`order_table`和`refund_table`时，`AND refund_table.status = 'success'`应放在`ON`中还是`WHERE`中（影响LEFT JOIN结果）；


## 第4步：WHERE行级过滤

**（1a）日期格式写法（仅索引，结果相同）**

- example数据库中，问题中的"2026年"，两种写法结果相同：`order_table.order_date BETWEEN '2026-01-01' AND '2026-12-31'`（区间比较可走索引）vs `YEAR(order_table.order_date) = 2026`（函数包裹索引失效），仅性能差异；
- financial数据库中，问题中的"1993年的账户"，两种写法结果相同：`date BETWEEN '1993-01-01' AND '1993-12-31'`（区间比较可走索引）vs `STRFTIME('%Y', date) = '1993'`（函数包裹索引失效），仅性能差异；
- codebase_community数据库中，问题中的"2014年9月1日之后登录的用户"，`LastAccessDate > '2014-09-01'`（直接比较，可走索引）vs `date(LastAccessDate) > '2014-09-01'`（date()函数包裹后索引失效），仅性能差异——但需注意后者会截断时间部分，若时间部分不为00:00:00则结果集有差异（见1b）；

**（1b）日期语义歧义（影响结果）**

- california_schools数据库中，问题中的"2000年之后开业的学校"，是指`OpenDate > '2000-01-01'`（2000年1月2日起算）还是`strftime('%Y', OpenDate) > '2000'`（2001年起算，排除整个2000年），取决于"之后"是否包含2000年全年；
- codebase_community数据库中，问题中的"2014年9月1日之后登录的用户"，`LastAccessDate > '2014-09-01'`（含当天时间部分，2014-09-01 00:00:01也算）还是`date(LastAccessDate) > '2014-09-01'`（截断时间后仅2014-09-02起算），取决于"9月1日之后"是否包含当天；
- student_club数据库中，问题中的"October Meeting on October 8, 2019"，是指`event_date LIKE '2019-10-08%'`（含时分秒模糊匹配）还是`event_date = '2019-10-08'`（按日期精确匹配），取决于event_date是否带时间分量；



**（2）单元格值匹配**

- card_games数据库中，问题中的"稀有度为uncommon的卡牌"，是指`rarity = 'uncommon'`（精确值匹配）还是`rarity LIKE '%uncommon%'`（模糊匹配，可能误匹配）；
- thrombosis_prediction数据库中，问题中的"异常抗SM抗体"，是指`SM NOT IN ('negative', '0')`（排除正常值）还是`SM = 'positive'`（仅匹配单一阳性值，可能遗漏边界值）；
- financial数据库中，问题中的"南波希米亚(South/East/North Bohemia)"，是指`district.A3 = 'south Bohemia'`（小写s）还是`district.A3 = 'South Bohemia'`（大写S，需确认实际存储值的大小写）；
- financial数据库中，问题中的"运行中合同(running contract)"，是指`loan.status IN ('C', 'D')`（含正常和欠款两种运行状态）还是仅`loan.status = 'C'`（仅正常状态）；
- toxicology数据库中，问题中的"致癌分子(carcinogenic molecules)"，是指`molecule.label = '+'`（+号表示致癌）还是`molecule.label = 'carcinogenic'`（英文全称）；
- toxicology数据库中，问题中的"单键(single-bonded molecules)"，是指`bond.bond_type = '-'`（化学单键短线符号）还是`bond.bond_type = 'single'`（英文单词）；
- toxicology数据库中，问题中的"双键(double bond)"，是指`bond_type = '='`（精确等号匹配）还是`bond_type LIKE '%double%'`（自然语言模糊匹配）；
- toxicology数据库中，问题中的"碳元素(carbon)"，是指`atom.element = 'c'`（小写化学符号）还是`atom.element = 'C'`（大写化学符号）；
- card_games数据库中，问题中的"秘稀稀有度(mythic rarity)"，是指`rarity = 'mythic'`（精确字符串）还是`rarity LIKE '%mythic%'`（模糊匹配）；
- card_games数据库中，问题中的"未来边框版本(future frame version)"，是指`frameVersion = 'future'`（字符串）还是`frameVersion = 2015`（数字年份）；



**（3）隐式条件**

- example数据库中，是否需要默认加`AND order_table.is_deleted = 0`过滤软删除数据；
- thrombosis_prediction数据库中，问题中的"患者检查记录"，是否需额外加`AND Symptoms IS NOT NULL`过滤掉无症状的记录；
- thrombosis_prediction数据库中，问题中的"正常范围内的尿酸值"，是指`UA > 8.0 AND SEX = 'M' OR UA > 6.5 AND SEX = 'F'`（分性别判断）还是统一用`UA BETWEEN 3.5 AND 7.2`（忽略性别差异）；
- formula_1数据库中，问题中的"拉普兰圈速记录"，是按`time`字段字符串排序，还是按`milliseconds`数值字段排序（需确认哪个字段有值且正确）；




## 第5步：GROUP BY分组

**（1）分组维度选择**

- example数据库中，问题中的"按部门统计"，是用`employee_table.dept_id`（精准）还是`dept_name`（可读，但有重名风险）；
- financial数据库中，问题中的"按年份统计数量"，是指`GROUP BY strftime('%Y', date)`（按年份分组）还是`GROUP BY date`（按完整日期分组，无法实现按年聚合）；
- financial数据库中，问题中的"女性账户持有人最多的前9个地区"，是指`GROUP BY district_id, A2`（地区ID+名称双字段确保唯一性）还是仅`GROUP BY A2`（地区名可能重名）；
- financial数据库中，问题中的"南波希米亚人口最多的分支地区的男性比例"，是指`GROUP BY A4`（按地区分组取人口最大的一个）还是无`GROUP BY`（计算全局比例而非指定地区）；
- california_schools数据库中，问题中的"统计每所学校的FRPM值"，是指按`CDSCode`（学校唯一编码，安全）分组还是按`School`（学校名称，可能有重名风险）分组；
- california_schools数据库中，问题中的"最常见的两个管理员名字及所属学区"，是指子查询内按`admfname1`分组计数取前2后与学校表关联，还是直接按`first_name, district`分组（多district维度导致同一名字在不同学区被多次计数）；
- card_games数据库中，问题中的"各类型卡牌的数量"，是指按`type`（卡牌类型）分组还是按`rarity`（稀有度）分组，需确认问题意图；
- card_games数据库中，问题中的"插画最少的插画师是谁及其卡牌玩法格式"，是指仅按`artist`分组（按插画师计卡牌数）还是按`artist, format`分组（多出format维度，每组记录数≠插画师总卡牌数）；
- card_games数据库中，问题中的"裁决信息最多的卡牌（是否促销印刷品）"，是指子查询按`artist`找裁决最多的插画师，还是按`name, artist, isPromo`分组（三字段分组过细，每组仅一条记录，COUNT无意义）；
- formula_1数据库中，问题中的"每站最快圈速"，是指按`circuitId`（赛道）分组后取`MIN(time)`，还是按`driverId`（车手）分组；
- formula_1数据库中，问题中的"各赛季冠军"，是指按`season`（赛季）分组还是按`round`（分站）分组；
- toxicology数据库中，问题中的"TR346分子中每个原子可以有哪些键类型"，是指按`atom_id, bond_type`分组（每个原子的每种键类型各一行）还是仅按`atom_id`分组（丢失bond_type维度）；
- toxicology数据库中，问题中的"最常见的原子元素组合的键占比"，是指按`bond_type`分组（按键类型找最常见）还是按`a1.element, a2.element`分组（按元素组合找最常见，语义完全不同）；



**（2）SELECT非聚合列冲突**

- example数据库中，`SELECT employee_table.name`但`GROUP BY employee_table.dept_id`时，`name`不在GROUP BY中，应加入GROUP BY还是用`MAX(name)`包裹；
- financial数据库中，问题中的"1996年1月按地区名排前十的取款"，Gold用`SELECT DISTINCT A2 ...`（无GROUP BY），Generate用`SELECT A2, SUM(amount) ... GROUP BY district_id`，`A2`既不在GROUP BY中也不在聚合函数内，返回该district_id的任意一个name值；
- toxicology数据库中，问题中的"双键最多的分子是否致癌"，Generate用`SELECT label ... GROUP BY molecule_id ORDER BY COUNT(*) DESC LIMIT 1`，`label`不在GROUP BY中，依赖GROUP BY后的任意值行为，Gold用子查询+JOIN避免了此问题；
- toxicology数据库中，问题中的"TR346分子中每个原子可以有哪些键类型"，Gold用`SELECT atom_id, COUNT(DISTINCT bond_type), molecule_id ... GROUP BY atom_id, bond_type`，`molecule_id`既不在GROUP BY中也不在聚合函数内（虽函数依赖atom_id，但SQL引擎不识别）；Generate用`SELECT atom_id, COUNT(DISTINCT bond_type) ... GROUP BY atom_id`；
- card_games数据库中，问题中的"含卡牌最多的俄语套牌"，Gold用`SELECT id ... GROUP BY baseSetSize ORDER BY baseSetSize DESC LIMIT 1`，`id`不在GROUP BY中，返回该baseSetSize组的任意一个id；Generate用`SELECT name ... ORDER BY baseSetSize DESC LIMIT 1`（无GROUP BY，语义正确）；




## 第6步：聚合函数

**（1）DISTINCT必要性**

- example数据库中，问题中的"下单客户数"，是用`COUNT(DISTINCT order_table.customer_id)`还是`COUNT(*)`；
- toxicology数据库中，问题中的"含钠原子的分子中非致癌的有多少"，Gold用`COUNT(DISTINCT T2.molecule_id)`（每分子去重计数），Generate用`COUNT(*)`（atom JOIN molecule后重复行全计）；
- toxicology数据库中，问题中的"致癌且有溴元素的分子数"，Gold用`COUNT(DISTINCT T2.molecule_id)`（分子级去重），Generate用`COUNT(*)`（atom JOIN molecule未去重，同一分子出现多次）；
- toxicology数据库中，问题中的"所有致癌分子中最不常见的元素"，Gold用`COUNT(DISTINCT T2.molecule_id)`（按分子去重统计元素出现次数），Generate用`COUNT(*)`（按原子行统计）；
- financial数据库中，问题中的"信用卡取款后月结账户中余额为负的账户数"，Gold用`COUNT(T1.account_id)`（account_id本身唯一，无需DISTINCT），Generate用`COUNT(DISTINCT t.account_id)`（多表关联后担心重复，但DISTINCT多余）；
- card_games数据库中，问题中的"稀有度为rare且裁决日期为2007-02-01的卡牌数"，是用COUNT(DISTINCT id)（去重）还是COUNT(*)（关联后可能重复）；
- example数据库中，问题中的"订单数"，是指`COUNT(DISTINCT order_table.order_id)`还是`COUNT(*)`；
- toxicology数据库中，问题中的"统计分子数量"，是指`COUNT(DISTINCT molecule_id)`（精确去重计数）还是`COUNT(*)`（含重复行的总数）；
- financial数据库中，问题中的"不重复的客户ID"，必须加`DISTINCT`关键字，否则因一对多关联产生重复行；

**（2）NULL处理**

- example数据库中，问题中的"平均工资"，是用`AVG(salary)`（忽略NULL）还是`SUM(salary)/COUNT(*)`（NULL视为0）；
- california_schools数据库中，问题中的"Locally funded学校与non-Locally funded学校的比例"，Gold用`SUM(CASE WHEN FundingType = 'Locally funded' THEN 1 ELSE 0 END)`（ELSE 0明确将非匹配行计为0）做除法，Generate用`COUNT(CASE WHEN FundingType = 'Locally funded' THEN 1 END) / COUNT(*)`（COUNT隐式ELSE NULL，非匹配行被忽略；分子相同但分母不同：前者仅统计FundingType非空行，后者`COUNT(*)`统计全部行）；
- financial数据库中，问题中的"南波希米亚地区的男性客户比例"，Gold用`SUM(T1.gender = 'M') / COUNT(T1.client_id)`（COUNT(client_id)忽略client_id为NULL的行），Generate用`COUNT(CASE WHEN c.gender = 'M' THEN 1 END) / COUNT(*)`（COUNT(*)包含所有行）；
- toxicology数据库中，问题中的"triple bond分子中碳原子的占比"，Gold用`COUNT(DISTINCT CASE WHEN T1.element = 'c' THEN T1.atom_id ELSE NULL END) / COUNT(DISTINCT T1.atom_id)`（ELSE NULL使非c原子的atom_id不参与DISTINCT计数），Generate用`SUM(CASE WHEN a.element = 'c' THEN 1 ELSE 0 END) / COUNT(*)`（分子分母与Gold完全不同，后者COUNT(*)不按atom_id去重）；
- toxicology数据库中，问题中的"非氟原子分子的致癌比例"，Gold用`COUNT(DISTINCT CASE WHEN T1.element <> 'f' THEN T2.molecule_id ELSE NULL END) / COUNT(DISTINCT T2.molecule_id)`（ELSE NULL排除含氟分子的molecule_id），Generate用`SUM(CASE WHEN has_fluorine = 0 THEN 1 ELSE 0 END) / COUNT(*)`（非原子级去重）；
- card_games数据库中，问题中的"white边框卡牌中power含*或NULL的比例"，Gold用`SUM(CASE WHEN power LIKE '%*%' OR power IS NULL THEN 1 ELSE 0 END) / COUNT(id)`（SUM + CASE + ELSE 0显式处理NULL条件），Generate用`COUNT(*) WHERE ... (power = '*' OR power IS NULL)`（WHERE直接过滤而非在聚合中处理NULL分支）；




## 第7步：HAVING分组后过滤

**（1）WHERE vs HAVING放置位置**

- example数据库中，问题中的"平均工资大于10000的部门"，用`HAVING AVG(salary) > 10000`；而"工资高于10000的员工所在的部门"，先用`WHERE salary > 10000`再`GROUP BY`；




## 第8步：ORDER BY排序

**（1）NULL值排序位置**

- example数据库中，`ORDER BY employee_table.salary DESC`时，是否需显式加`NULLS LAST`（不同数据库默认行为不同）；



**（2）多字段排序优先级**

- example数据库中，问题中的"按部门和工资排序"，对应`ORDER BY dept_id, salary`；
- financial数据库中，问题中的"贷款额最高的客户的第一笔交易"，是指`ORDER BY amount DESC, date ASC`（先按金额降序找到最大贷款，再按日期升序取第一笔）还是仅`ORDER BY amount DESC`（只取最大贷款对应的一行，但不是第一笔交易）；




## 第9步：LIMIT分页

**（1）必须伴随ORDER BY**

- example数据库中，问题中的"前10个订单"，若未指定排序字段，是否应默认加`ORDER BY create_time DESC`，否则返回随机10条；




## 第10步：嵌套子查询拆解

**（1）是否嵌套（子查询 / 派生表 / CTE）**

- formula_1数据库中，问题中的"21世纪前10年年均比赛数"，是指先`GROUP BY year`按年分组再求平均（派生表，`SUM(race_count)/COUNT(*)`），还是直接`SUM(CASE WHEN ...)/10`计算（无嵌套），两种写法等价；
- toxicology数据库中，问题中的"致癌分子中不含氟的占比"，是指先通过派生表给每个分子打`has_fluorine`标记再汇总（嵌套），还是用`COUNT(DISTINCT CASE WHEN element <> 'f' THEN molecule_id END) / COUNT(DISTINCT molecule_id)`直接算（无嵌套），两种写法等价；
- debit_card_specializing数据库中，问题中的"年消费不足30000的KAM客户数"，应该用派生表先`GROUP BY CustomerID HAVING SUM(Consumption) < 30000`再外包`COUNT(*)`，不应直接在行级过滤`Consumption < 30000`（后者将人均消费误解为单笔消费）；
- toxicology数据库中，问题中的"单键分子中平均氧原子数"，应该用派生表先按分子`COUNT(oxygen)`分组再`AVG`，不应直接在行级`AVG(CASE WHEN element = 'o' THEN 1 ELSE 0 END)`（后者分母为原子行数而非分子数，语义错误）；
- codebase_community数据库中，问题中的"同时拥有Teacher和Supporter两个徽章的用户数"，应该用子查询`GROUP BY UserId HAVING COUNT(DISTINCT Name) = 2`确保两徽章同时持有，不应只用`Name IN ('Teacher', 'Supporter')`（后者只需任一徽章即匹配，逻辑错误）；




## 第11步：UNION / UNION ALL

**（1）去重与否**

- example数据库中，默认使用`UNION ALL`（不去重，性能更优）还是`UNION`（去重，需用户明确要求）；




## 第12步：性能陷阱

**（1）函数包裹索引字段**

- example数据库中，`WHERE DATE(create_time) = '2026-07-09'`应改为`create_time BETWEEN '2026-07-09 00:00:00' AND '2026-07-09 23:59:59'`，否则索引失效；




## 第13步：输出列格式

**（1）列别名**

- example数据库中，用户问"总销售额"，输出列别名应为`total_sales`而非`SUM(amount)`；
- financial数据库中，问题中的"客户信息"，是指仅输出`CustomerID`（必要列）还是额外输出`Gender, BirthDate, District`等非问题要求的列；
- european_football_2数据库中，问题中的"球队构建速度"，是指仅输出`buildUpPlaySpeed`（速度值）还是同时输出`team_long_name`（球队名）+速度值；
- student_club数据库中，问题中的"预算ID"，是指输出`budget_id`（预算编号）还是`MAX(amount)`（金额最大值），需确认问题问的是ID还是金额；


**（2）ROUND精度**

- california_schools数据库中，问题中的"百分比"，是指`Free Meal Count * 100 / Enrollment`（自行计算并转百分比）还是直接取`Percent (%) Eligible Free`预存百分比字段（两者数值相同但来源不同）；
- toxicology数据库中，问题中的"比例值"，是指`ROUND(CAST(value AS REAL), 2)`（保留两位小数）还是`CAST(value AS REAL)`（原始精度）；

