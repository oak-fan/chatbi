# 一、总体方案


## 1.1 核心思想

"事中验证"**：在SQL生成过程中，将自然语言问题拆解为若干独立的**决策单元**，对每个决策单元通过**工具举证**获取客观证据，即时裁决后拼接出最终SQL。


## 1.2 整体处理流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            整体处理流程                                     │
└─────────────────────────────────────────────────────────────────────────────┘

                            ┌──────────────┐
                            │  用户自然语言  │
                            │   问题输入    │
                            └──────┬───────┘
                                   ↓
                   ┌───────────────────────────────┐
                   │   第1阶段：意图拆解             │
                   │   将问题拆解为若干决策单元       │
                   │   （每个决策单元对应一个歧义点）  │
                   └───────────────┬───────────────┘
                                   ↓
                   ┌───────────────────────────────┐
                   │   第2阶段：拓扑排序             │
                   │   按依赖关系排列决策单元执行顺序  │
                   │   （先决条件优先，独立单元并行）  │
                   └───────────────┬───────────────┘
                                   ↓
                   ┌───────────────────────────────┐
                   │   第3阶段：逐单元举证与裁决      │
                   │   对每个决策单元：               │
                   │   ├─ 生成2-3个候选片段          │
                   │   ├─ 调用工具获取证据            │
                   │   ├─ 根据证据裁决选择            │
                   │   └─ 记录证据链                 │
                   └───────────────┬───────────────┘
                                   ↓
                   ┌───────────────────────────────┐
                   │   第4阶段：SQL拼接与最终验证     │
                   │   将所有已裁决片段拼接为完整SQL   │
                   │   执行完整SQL验证结果合理性       │
                   └───────────────┬───────────────┘
                                   ↓
                            ┌──────────────┐
                            │  输出最终SQL  │
                            │  + 证据链报告 │
                            └──────────────┘
```


## 1.3 处理流程详述

### 第1阶段：意图拆解

- **输入**：用户自然语言问题 + 数据库Schema信息
- **输出**：决策单元列表
- **方法**：根据问题中的关键词和短语，对照错误点清单，识别所有可能存在歧义或容易出错的位置，将每个位置封装为一个独立的决策单元
- **示例**："统计2026年每个部门活跃用户的订单总金额" → 拆解为时间范围、活跃定义、金额字段、部门分组、JOIN类型、聚合粒度共6个决策单元


### 第2阶段：拓扑排序

- **输入**：决策单元列表
- **输出**：排好执行顺序的决策单元列表
- **方法**：识别决策单元之间的依赖关系（例如：表选择决策必须在字段选择决策之前），按依赖关系进行拓扑排序；无依赖关系的决策单元标记为可并行执行
- **原则**：
  - `FROM表选择` → `JOIN关联` → `WHERE过滤` → `SELECT字段` → `GROUP BY分组` → `HAVING过滤` → `ORDER BY排序` → `LIMIT分页`
  - 业务口径类决策（如"活跃用户定义"）可独立并行，不依赖其他决策
- **回溯基础**：依赖图同时记录了每个单元的下游节点。后续环节发现问题时，可据此精确计算影响范围，只撤销并重跑受影响单元，避免全局重来。


### 第3阶段：逐单元举证与裁决

- **输入**：按序排列的决策单元
- **输出**：每个决策单元的裁决结果 + 证据链
- **方法**：每个决策单元执行"LLM推理 + 代码执行"的举证循环：
  1. **LLM生成候选 + 验证计划**：根据决策类型 + Schema，生成2-3个候选片段，并为每个候选写出需执行的SQL验证语句（非硬编码，按实际字段名自适应）
  2. **执行验证计划**：确定性代码按低成本优先依次执行LLM指定的工具调用，收集结果
  3. **LLM解读 + 裁决**：根据验证结果选择最优候选；证据不足时标记"待用户确认"
  4. **记录证据链**：确定性代码保存裁决依据
- **成本控制**：按"低成本工具优先、高成本工具后置"的顺序调用；简单决策直接裁决，仅复杂决策才调用重工具
- **回溯机制**：裁决当前单元时，若发现前置单元的证据不足或裁决有误，可撤销该前置单元及所有下游的结果，重新举证裁决。DAG确保了影响范围可精确计算，而非全局重跑。


### 第4阶段：SQL拼接与最终验证

- **输入**：所有决策单元的裁决结果
- **输出**：最终SQL + 证据链报告
- **方法**：
  1. 按SQL语法结构将所有已裁决片段拼接为完整SQL
  2. 在测试环境执行完整SQL，检查：
     - 是否有语法错误
     - 返回行数是否合理（非0行，非全表暴增）
     - 执行时间是否可接受
  3. 生成证据链报告，说明每个决策点的候选、证据和选择理由
- **回溯机制**：若最终验证失败（语法错误、结果不合理、性能不可接受）：
   1. 分析失败原因，定位到具体决策单元（如JOIN类型选错导致行数异常）
   2. 查询依赖图，确定该单元的所有下游
   3. 撤销该单元及所有下游的裁决结果，保留其他单元的裁决
   4. 从该单元开始重新执行举证与裁决
   5. 重跑完毕后重新拼接验证
   6. 最多重试2次，超限则报错退出


## 1.4 决策单元之间的拓扑结构

决策单元之间存在天然的依赖关系，决定了它们的执行顺序：

```
                          ┌──────────────────────────────────┐
                          │       ① 业务口径                 │
                          │  活跃定义、销售额口径、学校类型等  │
                          │  高频分歧，独立决策，影响所有下游  │
                          └──────────────┬───────────────────┘
                                         │
                                         ▼
                          ┌──────────────────────────────────┐
                          │       ③ 表源                     │
                          │  FROM表选择 / JOIN关联            │
                          │  低频分歧，仅同义表/多表场景触发   │
                          └──────────────┬───────────────────┘
                                         │
                                         ▼
                          ┌──────────────────────────────────┐
                          │       ④ 过滤（WHERE）            │
                          │  行级条件：日期/值匹配/隐式条件    │
                          │  中频分歧，视查询复杂度按需触发    │
                          └──────────────┬───────────────────┘
                                         │
                                         ▼
                          ┌──────────────────────────────────┐
                          │       ② SELECT列                 │
                          │  列选择 / 去重 / 计算字段         │
                          │  高频分歧，清单中分歧点最多的环节  │
                          └──────────────┬───────────────────┘
                                         │
                                         ▼
                          ┌──────────────────────────────────┐
                          │       ⑤ 分组聚合                  │
                          │  GROUP BY维度 / 聚合函数选择      │
                          │  高频分歧，分组字段歧义/DISTINCT  │
                          └──────────────┬───────────────────┘
                                         │
                      ┌──────────────────┼──────────────────┐
                      │                  │                  │
                      ▼                  ▼                  ▼
               ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
               │④ 过滤(HAVING)│  │ ⑥ 排序输出   │  │② 输出格式   │
               │分组后过滤    │  │ ORDER BY /   │  │别名/精度    │
               │极少分歧      │  │ LIMIT        │  │偶尔分歧     │
               └──────────────┘  │ 极少分歧      │  └──────────────┘
                                 └──────────────┘
```

**说明**：每次查询不实例化全部节点，仅当清单匹配到该类型的分歧点时，才生成对应的决策单元。例如简单查询"SELECT COUNT(*) FROM orders"无匹配分歧点，不进入举证链。依赖关系决定的是"如果这个决策单元存在，它的执行位置和影响范围"。

**类型内子环节的依赖细节（对需要细化拆解的环节）：**

| 依赖关系 | 原因 |
|---------|------|
| 业务口径 → 所有下游 | 口径决定用哪个字段，影响后续所有选择 |
| 表源(FROM) → 表源(JOIN) | 不知道主表，无法确定JOIN对象 |
| 表源 → 过滤(WHERE) | 知道表后，才能确定可过滤的列 |
| 表源 → SELECT列 | SELECT的字段必须来自已确定的表 |
| 过滤(WHERE) → SELECT列 | 行过滤后的结果集决定SELECT可见数据 |
| SELECT列 → 分组聚合 | GROUP BY的字段必须是SELECT中的非聚合字段 |
| 分组聚合 → 过滤(HAVING) | HAVING是对分组后的结果过滤 |
| SELECT列 → 排序输出 | ORDER BY只能排序已选择的列或聚合 |
| 分组聚合 → 排序输出 | ORDER BY可以使用聚合结果排序 |
| SELECT列 → 输出格式 | 列别名/精度由选择的列决定 |


## 1.5 举证工具设计

### 工具总览

本方案设计了**6类举证工具**，覆盖证据驱动的全流程：

| 工具类别 | 工具名称 | 用途 | 成本等级 |
|---------|---------|------|---------|
| **业务知识类** | HKB检索工具 | 查询业务规则文档、口径定义、字段映射规范、命名规范 | 低 |
| **元数据类** | Schema查询工具 | 查询字段类型、长度、是否可空、默认值、字段注释（COMMENT） | 低 |
| **元数据类** | 外键/索引查询工具 | 查询表间外键关系、索引定义，辅助判断Join路径 | 低 |
| **数据查询类** | 数据抽样工具 | 查看表的前N行、字段样例值，了解数据格式和编码习惯 | 中 |
| **数据查询类** | SQL执行工具 | 执行各类SELECT查询获取数据事实。模式：`analyze`（COUNT/DISTINCT/聚合验证，5s超时）、`explain`（执行计划分析，10s超时）、`query`（通用复杂查询，30s超时） | 按模式：analyze/explain 中，query 高 |
| **用户交互类** | 用户澄清工具 | 当所有证据无法裁决时，生成澄清问题向用户确认 | 高（需人工） |

### 工具调用策略（成本感知）

决策类型按举证复杂度分为两类，走不同流程：

**模式一：简单举证**（适用于候选有限可枚举的决策，如去重、WHERE、JOIN、聚合等）
- 候选由LLM一次性列举，按低成本优先分层调用工具，每层结束后由同一LLM裁决

```
 第1层（必调，低成本）：
     ├── HKB检索工具（查业务定义）
     └── Schema查询工具（查字段注释和类型）
          ↓
     裁决→ 够 → 结束 | 不够 → 下一层

 第2层（按需，中成本）：
     ├── 数据抽样工具（看样例值）
     ├── SQL执行工具 analyze/explain
     └── 外键/索引查询工具
          ↓
     裁决→ 够 → 结束 | 不够 → 下一层

 第3层（少数调用，高成本）：
     └── SQL执行工具 query 模式
          ↓
     裁决→ 够 → 结束 | 不够 → 用户澄清
```

**模式二：复杂举证**（适用于业务口径、GROUP BY分组维度等开放式候选决策）
- 候选需从Schema中自动发现而非预先枚举；采用Generator+Judge双角色机制
- 不采用"逐层提前结束"策略，因开放式候选无法逐层判断是否覆盖完全

```
 Generator LLM + Schema工具多轮交互
   提取术语 → 扫描字段注释/值域 → 列举所有候选 → 生成验证SQL
   ↓
 执行全部验证计划（一次性调用所有必要工具收集证据）
   ↓
 Judge LLM 独立裁决（基于全部证据判断各候选证据强度）
   ↓
 证据不足 → 用户澄清 | 足够 → 记录证据链
```

### 各决策单元常用工具映射

| 复杂度 | 决策类型（对应错误点清单章节） | 首选工具 | 备选工具 |
|--------|-----------------------------|---------|---------|
| **复杂** | SELECT业务口径（第1步1a） | HKB检索 + Schema扫描 | 数据抽样 |
| **复杂** | GROUP BY分组维度（第5步1） | SQL执行工具 analyze（查重名） | 数据抽样 |
| 简单 | SELECT去重语义（第1步1b） | SQL执行工具 analyze（COUNT vs COUNT DISTINCT） | 数据抽样 |
| 简单 | SELECT计算字段（第1步1c） | HKB检索 | Schema查询 |
| 简单 | FROM表选择（第2步） | Schema查询 | HKB检索 |
| 简单 | JOIN类型（第3步1） | SQL执行工具 analyze（NULL计数） | 数据抽样 |
| 简单 | JOIN关联条件（第3步2） | 外键/索引查询 | 数据抽样 |
| 简单 | WHERE日期格式（第4步1a） | SQL执行工具 explain | HKB检索 |
| 简单 | WHERE日期语义（第4步1b） | SQL执行工具 analyze（MIN/MAX查看时间精度） | 数据抽样 |
| 简单 | WHERE单元格值匹配（第4步2） | 数据抽样 | SQL执行工具 analyze（DISTINCT值分布） |
| 简单 | WHERE隐式条件（第4步3） | Schema查询（看是否可空） | 数据抽样 |
| 简单 | SELECT非聚合列冲突（第5步2） | Schema查询 | - |
| 简单 | 聚合DISTINCT必要性（第6步1） | SQL执行工具 analyze（COUNT vs COUNT DISTINCT） | - |
| 简单 | 聚合NULL处理（第6步2） | SQL执行工具 analyze（COUNT vs COUNT(*)） | Schema查询 |
| 简单 | WHERE vs HAVING（第7步） | HKB检索 | - |
| 简单 | ORDER BY排序（第8步） | HKB检索 | 数据抽样 |
| 简单 | LIMIT必须伴随ORDER BY（第9步） | HKB检索 | - |
| 简单 | 嵌套子查询拆解（第10步） | SQL执行工具 analyze（对比两种写法结果） | SQL执行工具 explain |
| 简单 | UNION vs UNION ALL（第11步） | HKB检索 | - |
| 简单 | 性能陷阱（第12步） | SQL执行工具 explain | - |
| 简单 | 输出列别名/精度（第13步） | HKB检索 | - |

**复杂度判别依据**：**复杂**类型的候选集开放不可枚举，需从Schema中自动发现候选，适用Generator+Judge双角色模式。**简单**类型的候选集有限且可预先枚举（通常2-3个确定选项），由同一LLM完成生成和裁决。


# 二、处理样例


## 样例一：电商场景

### 输入

**自然语言问题：**
> "统计2026年每个部门活跃用户的订单总金额"

**数据库Schema（简化）：**
- `user_table`：user_id, dept_id, login_count, order_count
- `order_table`：order_id, user_id, amount, total_amount, order_date
- `dept_table`：dept_id, dept_name


### 处理过程

| 步骤 | 决策点 | 候选A | 候选B | 举证工具 | 证据 | 裁决 |
|------|--------|--------|--------|---------|------|------|
| **1** | 活跃用户定义 | `login_count > 0` | `order_count > 0` | **HKB检索工具**（查询"活跃用户的业务定义"） | 返回业务文档："活跃用户定义为近30天有登录行为的用户" | **选A** |
| **2** | 金额字段 | `amount`（实付金额） | `total_amount`（含税总价） | **HKB检索工具**（查询"订单总金额使用哪个字段"）；**Schema查询工具**（查看字段注释） | HKB："统计销售额使用实付金额amount"；Schema注释："amount=实付金额，total_amount=含税总价" | **选A** |
| **3** | 时间范围写法 | `BETWEEN '2026-01-01' AND '2026-12-31'` | `YEAR(order_date)=2026` | **SQL执行工具 explain 模式**（对比两种写法执行计划）；**HKB检索工具**（查询日期过滤最佳实践） | 执行计划：前者走索引range scan，后者全表扫描；HKB："建议用BETWEEN避免函数包裹索引字段" | **选A** |
| **4** | 部门分组键 | `dept_id` | `dept_name` | **SQL执行工具 analyze 模式**（查询"dept_name是否有重复值"） | `SELECT dept_name, COUNT(DISTINCT dept_id) FROM dept_table GROUP BY dept_name HAVING COUNT(DISTINCT dept_id)>1` → 返回2条重复（"财务部"对应id=3和id=7） | **选A** |
| **5** | JOIN类型 | `INNER JOIN user_table` | `LEFT JOIN user_table` | **SQL执行工具 analyze 模式**（查询"无订单用户的销售额如何统计"） | 问题要求"每个部门"的统计，有部门但无订单的用户销售额应为0，需保留 → 实际指向LEFT JOIN | **选B** |
| **6** | 聚合粒度 | `SUM(amount)` | `COUNT(order_id)` | **HKB检索工具**（查询"统计总金额是求和还是计数"） | HKB："订单总金额=SUM(金额)，COUNT为订单笔数" | **选A** |

**执行记录（第5步的补充证据）：**

```
调用：SQL执行工具 analyze 模式
查询：SELECT COUNT(*) FROM user_table u LEFT JOIN order_table o ON u.user_id=o.user_id AND o.order_date BETWEEN '2026-01-01' AND '2026-12-31' WHERE o.order_id IS NULL
返回：523（有523个用户在2026年无订单）
裁决：确认需要LEFT JOIN，否则这523个用户会被遗漏
```


### 输出

**最终SQL：**

```sql
SELECT 
    d.dept_id,
    d.dept_name,
    SUM(o.amount) AS total_sales
FROM dept_table d
LEFT JOIN user_table u ON d.dept_id = u.dept_id
LEFT JOIN order_table o ON u.user_id = o.user_id 
    AND o.order_date BETWEEN '2026-01-01' AND '2026-12-31'
WHERE u.login_count > 0
GROUP BY d.dept_id, d.dept_name
```

**证据链报告：**

| 决策点 | 候选 | 证据来源 | 证据摘要 | 裁决理由 |
|--------|------|---------|---------|----------|
| 活跃用户定义 | login_count>0 | HKB | 业务文档定义"活跃=有登录" | 符合业务口径 |
| 金额字段 | amount | HKB + Schema | 文档+注释均指向实付金额 | 符合统计规范 |
| 日期范围 | BETWEEN | SQL执行工具 explain | 前者走索引，后者全表扫描 | 性能优先 |
| 分组键 | dept_id | SQL执行工具 analyze | dept_name存在重名 | 避免歧义 |
| JOIN类型 | LEFT JOIN | SQL执行工具 analyze | 523名用户无订单，需保留 | 保证"每个部门"全覆盖 |
| 聚合方式 | SUM | HKB | "总金额"=求和 | 语义匹配 |


## 样例二：学校数据库场景

### 输入

**自然语言问题：**
> "列出加州Fresno地区高中水平特许学校的平均数学成绩"

**数据库Schema（简化）：**
- `school_table`：CDSCode, School, City, County, District, Charter, FundingType, EILCode, GSserved
- `satscores_table`：CDSCode, AvgScrMath, NumTstTakr


### 处理过程

| 步骤 | 决策点 | 候选A | 候选B | 候选C | 举证工具 | 证据 | 裁决 |
|------|--------|--------|--------|--------|---------|------|------|
| **1** | Fresno地区定位 | `City = 'Fresno'` | `County = 'Fresno'` | `District = 'Fresno Unified'` | **数据抽样工具**（查看三个字段样例值）；**SQL执行工具 analyze 模式**（查各字段"Fresno"记录数） | 抽样：City存"Fresno"（城市名），County存"Fresno"（县名），District存"Fresno Unified"（学区名）；COUNT：City=142条，County=387条，District=89条。"Fresno地区"通常指县 | **选B** |
| **2** | 特许学校定义 | `Charter = 1` | `FundingType = 'Directly funded'` | - | **HKB检索工具**（查询"特许学校的判断标准"）；**SQL执行工具 analyze 模式**（查询两条件交集） | HKB："特许学校=Charter School (Y/N)=1"；SQL：Charter=1有120所，FundingType匹配有118所，交集115所，3所仅FundingType匹配但Charter≠1 | **选A** |
| **3** | 高中水平定义 | `EILCode = 'HS'` | `GSserved LIKE '%9-12%'` | - | **SQL执行工具 analyze 模式**（查询两条件筛选集合的差集） | EILCode='HS'有85所，GSserved含'9-12'有82所，差集为5所（EILCode标记高中但GSserved不含9-12），经查为Alternative HS | **选A**（EILCode更权威） |
| **4** | 数学成绩字段 | `AvgScrMath` | 自行`AVG(ScrMath)` | - | **Schema查询工具**（查看AvgScrMath定义） | 注释："AvgScrMath为该校所有参加考试学生的数学平均分" | **选A** |
| **5** | 输出粒度 | `GROUP BY School`（按学校列出） | 不分组（全县一个平均值） | - | **HKB检索工具**（查询"列出学校"的语义） | HKB："'列出学校'表示按学校逐条输出" | **选A** |
| **6** | 空值处理 | `INNER JOIN satscores` | `LEFT JOIN satscores` | - | **SQL执行工具 analyze 模式**（查询"无成绩记录的学校数量"） | 符合条件的学校中，有7所无satscores记录；"列出学校"隐含全部列出 | **选B** |
| **7** | 最终验证 | 执行完整SQL | - | - | **SQL执行工具 query 模式**（执行完整候选SQL验证） | 返回78行，含7所学校AvgScrMath为NULL，执行时间0.3s，结果合理 | **通过** |

**执行记录（关键步骤）：**

```
第1步 - 调用：数据抽样工具
查询：SELECT City, County, District FROM school_table LIMIT 10
返回：City='Fresno'（城市名），County='Fresno'（县名），District='Fresno Unified'（学区名）
结论："Fresno地区"按常用语义指县（County）级别

第1步 - 调用：SQL执行工具 analyze 模式
查询：SELECT COUNT(*) FROM school_table WHERE City='Fresno'
返回：142
查询：SELECT COUNT(*) FROM school_table WHERE County='Fresno'
返回：387
查询：SELECT COUNT(*) FROM school_table WHERE District='Fresno Unified'
返回：89
结论：County范围最广，符合"地区"语义，选B

第2步 - 调用：HKB检索工具
查询：查询"特许学校的判断标准"
返回：业务文档定义"特许学校指Charter School (Y/N)=1的学校"

第3步 - 调用：SQL执行工具 analyze 模式
查询：SELECT COUNT(*) FROM school_table WHERE EILCode='HS' AND GSserved NOT LIKE '%9-12%'
返回：5（均为Alternative HS，EILCode标记更准确）

第6步 - 调用：SQL执行工具 analyze 模式
查询：符合条件的学校中，有多少所无satscores记录
返回：7
裁决：问题要求"列出"，应保留所有学校，选LEFT JOIN
```


### 输出

**最终SQL：**

```sql
SELECT 
    s.School,
    s.City,
    s.County,
    sa.AvgScrMath
FROM school_table s
LEFT JOIN satscores_table sa ON s.CDSCode = sa.CDSCode
WHERE s.County = 'Fresno'
    AND s.Charter = 1
    AND s.EILCode = 'HS'
ORDER BY s.School
```

**证据链报告：**

| 决策点 | 候选 | 证据来源 | 证据摘要 | 裁决理由 |
|--------|------|---------|---------|----------|
| Fresno定位 | County | 数据抽样 + SQL查询 | 字段值分布显示County='Fresno'有387条，最符合"地区"语义 | 覆盖范围匹配 |
| 特许学校 | Charter=1 | HKB + SQL查询 | 业务文档明确定义；与FundingType差集仅3条 | 业务口径优先 |
| 高中水平 | EILCode='HS' | SQL查询 | EILCode更准确，GSserved漏标5所 | 数据准确性优先 |
| 成绩字段 | AvgScrMath | Schema | 字段注释明确为预计算平均分 | 字段定义匹配 |
| 输出粒度 | GROUP BY School | HKB | "列出学校"语义=按学校输出 | 语义匹配 |
| 空值处理 | LEFT JOIN | SQL查询 | 7所学校无成绩，但需保留 | 保证"列出"全覆盖 |
| 最终验证 | 执行完整SQL | SQL执行工具 query | 返回78行，含NULL值，执行正常 | 语法正确，结果合理 |