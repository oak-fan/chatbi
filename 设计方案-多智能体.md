# ChatBI 多智能体 SQL 生成方案

## 1. 这条链路解决什么问题

ChatBI 的核心任务是把用户自然语言问题转换成 SQL。传统单智能体 Text-to-SQL 容易把多个隐含决策混在一次生成里，例如：

- “客户数”到底是 `COUNT(*)`、`COUNT(customer_id)` 还是 `COUNT(DISTINCT customer_id)`；
- “每个部门”是否要保留没有订单的部门，也就是 JOIN 该用 `INNER JOIN` 还是 `LEFT JOIN`；
- “2026 年”对 timestamp 字段应该写成半开区间还是 `BETWEEN`；
- 问题里出现的字面值是否真的存在于数据库单元格里，大小写/拼写是否一致；
- 聚合层级到底是按 id 分组、按名称分组，还是 id + name 一起分组；
- NULL 是应该过滤、保留，还是用 `COALESCE` 转成 0。

多智能体链路的目标不是“让多个模型投票”，而是把 SQL 生成拆成几种不同视角的审查：

1. 有智能体负责生成多种候选 SQL。
2. 有智能体专门理解业务语义和聚合口径。
3. 有智能体专门挑 SQL 结构问题。
4. 有智能体专门基于真实数据库执行结果做验证。
5. 最后由裁决智能体基于证据选择或修正最终 SQL。

这条链路当前独立放在 `backend/app/services/system/chatbi/multi_agent` 下，不插入普通 `query` 问数流程，也不依赖 `dinsql`。它目前更适合作为 benchmark 方法或后续前端单独选择的方法。

## 2. 代码位置

核心代码：

- `backend/app/services/system/chatbi/multi_agent/__init__.py`
- `backend/app/services/system/chatbi/multi_agent/types.py`
- `backend/app/services/system/chatbi/multi_agent/prompts.py`
- `backend/app/services/system/chatbi/multi_agent/tools.py`
- `backend/app/services/system/chatbi/multi_agent/runner.py`

知识库构建脚本：

- `backend/scripts/build_bird_dev_knowledge_store.py`

benchmark 接入点：

- `backend/app/domain/system/chatbi/benchmark.py`
- `backend/app/services/system/chatbi/benchmark_service.py`

设计文档：

- `设计方案-多智能体.md`

## 3. 总体架构

当前链路入口是 `MultiAgentSqlRunner`。

```text
用户问题 + datasource
        |
        v
解析 datasource.db_schema
        |
        v
MultiAgentToolbox 初始化
        |
        v
knowledge_search 检索 BIRD database_description
        |
        v
SQL 生成智能体生成候选 SQL
        |
        +--------------------+
        |                    |
        v                    v
业务理解智能体          sql_probe 执行候选 SQL
        |                    |
        v                    v
SQL 结构审查智能体 <----- query/explain 证据
        |
        v
数据验证智能体
        |
        v
裁决智能体
        |
        v
最终 SQL
```

实际执行顺序在 `runner.py` 中是串行编排的：

1. `knowledge_search`
2. `sql_generator`
3. `business_agent`
4. `sql_probe`
5. `structure_review`
6. `data_validation`
7. `judge`

这样做是为了第一版可控、可追踪。后续如果要变成真正的 function-calling agent loop，可以把 `tools.py` 里的方法直接映射成工具 schema。

## 4. 关键对象

### MultiAgentSqlRunner

位置：`backend/app/services/system/chatbi/multi_agent/runner.py`

职责：

- 接收用户问题、datasource、datasource_owner_id、model；
- 从 `datasource.db_schema` 构造 `ChatbiDbSchemaRecord`；
- 把 schema 格式化成 LLM 可读文本；
- 初始化 `MultiAgentToolbox`；
- 依次调用各智能体；
- 汇总候选 SQL、各智能体输出、工具结果、token usage；
- 输出最终 SQL。

主要方法：

- `run(...)`：一次性运行并返回 `MultiAgentRunResult`。
- `run_stream(...)`：按 ChatBI SSE 事件形态输出中间过程。

### MultiAgentToolbox

位置：`backend/app/services/system/chatbi/multi_agent/tools.py`

职责：

- 给智能体链路提供统一工具；
- 工具输入输出尽量 JSON 友好；
- 复用现有 ChatBI 连接、SQL 执行、value founding、LLM embedding 能力。

当前工具：

- `knowledge_search`
- `sql_probe`
- `value_founding`

### prompts.py

位置：`backend/app/services/system/chatbi/multi_agent/prompts.py`

职责：

- 保存每个智能体的 `SYSTEM` prompt；
- 保存每个智能体的 `USER_PROMPT` 模板；
- 提供对应的 `build_*_user_prompt` 函数；
- 提供 JSON 解析和 SQL 清洗工具。

目前包含：

- `SQL_GENERATOR_SYSTEM`
- `SQL_GENERATOR_USER_PROMPT`
- `BUSINESS_AGENT_SYSTEM`
- `BUSINESS_AGENT_USER_PROMPT`
- `STRUCTURE_REVIEW_SYSTEM`
- `STRUCTURE_REVIEW_USER_PROMPT`
- `DATA_VALIDATION_SYSTEM`
- `DATA_VALIDATION_USER_PROMPT`
- `JUDGE_SYSTEM`
- `JUDGE_USER_PROMPT`

## 5. 工具详解

### 5.1 knowledge_search

用途：

从 BIRD-DEV 的 `database_description` 构建的向量知识库里检索字段解释、值解释、业务含义、常识 evidence。

为什么需要：

BIRD 题目很多答案依赖字段描述或 value_description。例如 California schools 里的 `Charter School (Y/N)`，数据库里可能是 0/1，但自然语言问的是 charter schools。没有 description，模型容易猜成字符串条件。

构建脚本：

```text
backend/scripts/build_bird_dev_knowledge_store.py
```

默认读取：

```text
../BIRD-DEV/dev_databases/*/database_description/*.csv
```

默认写入：

```text
postgresql+asyncpg://postgres:123456@47.94.248.19:18004/postgres
```

默认表：

```text
bird_dev_knowledge_chunks
```

表内核心字段：

- `chunk_id`：分段 id；
- `db_name`：BIRD 数据库名，例如 `california_schools`；
- `table_name`：表名；
- `source_path`：来源 CSV 路径；
- `content`：分段文本；
- `metadata`：db/table/source/chunk_index；
- `embedding`：pgvector 向量。

运行方式：

```bash
cd backend
.venv/bin/python scripts/build_bird_dev_knowledge_store.py --bird-dev-root ../BIRD-DEV/dev_databases
```

可选参数：

```text
--database-url
--batch-size
--chunk-size
--reset
```

运行时依赖：

- 目标 PostgreSQL 需要安装 pgvector；
- 后端 LLM embedding 配置需要可用；
- 默认按当前项目的 `LLMService.aembedding` 调用 embedding 模型。

检索逻辑：

1. 对 query 调用 `LLMService.aembedding`；
2. 连接知识库 PostgreSQL；
3. 按 `db_name = 当前 datasource schema.database` 过滤；
4. 使用 `embedding <=> query_embedding` 排序；
5. 返回 top_k chunk。

输出示例：

```json
{
  "chunk_id": "...",
  "db_name": "california_schools",
  "table_name": "schools",
  "source_path": "california_schools/database_description/schools.csv",
  "content": "Database: california_schools\nTable: schools\n...",
  "score": 0.82,
  "metadata": {"table_name": "schools"}
}
```

### 5.2 sql_probe

用途：

让智能体写 SQL 给真实数据库执行，获得证据，而不是只靠语言模型互相争论。

支持模式：

- `query`：执行 SQL，返回结果预览；
- `explain`：执行 explain，返回执行计划。

输入：

```json
{
  "sql": "SELECT ...",
  "mode": "query",
  "max_rows": 30
}
```

输出：

```json
{
  "mode": "query",
  "sql": "SELECT ...",
  "success": true,
  "columns": ["..."],
  "rows": [{"...": "..."}],
  "row_count": 12,
  "truncated": false,
  "error": null
}
```

安全边界：

- 底层复用 `ChatbiDbConnectionService.execute_readonly_sql`；
- 仍走现有只读 SQL 校验；
- 当前主要按 BIRD-DEV 数据集中的 SQLite 数据源适配；
- `explain` 模式下 SQLite 使用 `EXPLAIN QUERY PLAN`，其他方言使用 `EXPLAIN`。

典型用途：

- 判断候选 SQL 是否可执行；
- 比较 `COUNT(*)` 和 `COUNT(DISTINCT id)` 的结果差异；
- 比较 `INNER JOIN` 和 `LEFT JOIN` 是否导致行数变化；
- 检查过滤值是否返回空结果；
- 观察 GROUP BY 后的行粒度是否符合问题。

### 5.3 value_founding

用途：

给定表名、列名和字面值，在该列中查找相似单元格值。

输入：

```json
{
  "table_name": "schools",
  "column_name": "City",
  "literal": "Fresno"
}
```

输出：

```json
[
  {
    "literal": "Fresno",
    "column_ref": "schools.City",
    "value": "Fresno",
    "score": 100.0
  }
]
```

实现方式：

- 复用 `backend/app/services/system/chatbi/query/value_founding.py` 的 `ValueFoundingMatcher`；
- 第一版要求调用方显式提供表名和列名；
- 不在工具内部让 LLM 自动猜列，避免把字段选择和单元格匹配混在一起。

## 6. 智能体详解

### 6.1 SQL 生成智能体

System prompt：

```text
SQL_GENERATOR_SYSTEM
```

User prompt：

```text
SQL_GENERATOR_USER_PROMPT
```

输入上下文：

- database type；
- schema；
- user question；
- knowledge_search hits。

任务：

- 生成 2-5 条候选 SQL；
- 候选之间要有受控差异，不是随便改写；
- 显式探索 JOIN 类型、DISTINCT、聚合层级、日期边界、NULL 处理、字面值匹配等分歧点；
- 每条 SQL 必须带假设和证据。

输出结构：

```json
{
  "candidates": [
    {
      "id": "c1",
      "sql": "SELECT ...",
      "assumptions": ["..."],
      "divergence_points": ["JOIN_TYPE", "DISTINCT_NECESSITY"],
      "evidence": [{"source": "schema", "detail": "..."}]
    }
  ]
}
```

### 6.2 业务理解智能体

System prompt：

```text
BUSINESS_AGENT_SYSTEM
```

User prompt：

```text
BUSINESS_AGENT_USER_PROMPT
```

输入上下文：

- database type；
- schema；
- user question；
- knowledge_search hits；
- candidate SQLs。

任务：

- 判断业务口径；
- 判断去重口径；
- 判断是否需要计算字段；
- 判断日期语义；
- 判断隐式条件；
- 判断分组维度；
- 判断 DISTINCT 和 NULL 的业务含义；
- 对每个候选 SQL 给 support / risks。

输出结构：

```json
{
  "constraints": {
    "metric": "...",
    "dedup_scope": "...",
    "grouping": "..."
  },
  "candidate_feedback": [
    {"candidate_id": "c1", "support": ["..."], "risks": ["..."]}
  ],
  "evidence": [{"source": "knowledge", "detail": "..."}],
  "uncertainties": ["..."]
}
```

### 6.3 SQL 结构审查智能体

System prompt：

```text
STRUCTURE_REVIEW_SYSTEM
```

User prompt：

```text
STRUCTURE_REVIEW_USER_PROMPT
```

输入上下文：

- database type；
- schema；
- user question；
- business analysis；
- candidate SQLs；
- sql_probe results。

任务：

- 检查 SQL 是否可执行；
- 检查表列是否存在；
- 检查 JOIN 路径是否合法；
- 检查 JOIN 类型是否符合问题；
- 检查 LEFT JOIN 是否被 WHERE 条件意外变成 INNER JOIN；
- 检查 GROUP BY 与 SELECT 非聚合列是否冲突；
- 检查 DISTINCT、NULL、日期条件、LIMIT/ORDER BY 等风险。

输出结构：

```json
{
  "reviews": [
    {
      "candidate_id": "c1",
      "validity": "valid|risky|invalid",
      "risks": ["..."],
      "fix_suggestions": ["..."],
      "evidence": [{"source": "sql_probe", "detail": "..."}]
    }
  ]
}
```

### 6.4 数据验证智能体

System prompt：

```text
DATA_VALIDATION_SYSTEM
```

User prompt：

```text
DATA_VALIDATION_USER_PROMPT
```

输入上下文：

- database type；
- schema；
- user question；
- candidate SQLs；
- sql_probe results。

任务：

- 比较候选 SQL 的执行成功/失败；
- 比较返回列、行数、行粒度；
- 分析候选差异来自 JOIN、DISTINCT、过滤、GROUP BY 还是 NULL；
- 标记可疑空结果；
- 注意预览结果只是证据，不是完整真相。

输出结构：

```json
{
  "comparisons": ["..."],
  "candidate_feedback": [
    {"candidate_id": "c1", "support": ["..."], "risks": ["..."]}
  ],
  "evidence": [{"source": "sql_probe", "detail": "..."}]
}
```

### 6.5 裁决智能体

System prompt：

```text
JUDGE_SYSTEM
```

User prompt：

```text
JUDGE_USER_PROMPT
```

输入上下文：

- database type；
- schema；
- user question；
- candidate SQLs；
- business analysis；
- structure review；
- data validation；
- tool results。

任务：

- 不是简单投票；
- 综合业务语义、schema 合法性、SQL 结构风险、执行证据；
- 选择最佳候选；
- 必要时对最佳候选做小修正；
- 输出最终 SQL 和剩余不确定性。

输出结构：

```json
{
  "winner": "c1",
  "final_sql": "SELECT ...",
  "confidence": 0.82,
  "reason": "brief reason",
  "evidence": [{"source": "business_agent", "detail": "..."}],
  "remaining_uncertainties": []
}
```

## 7. 证据如何流转

第一版证据流是显式 JSON 传递：

1. `knowledge_search` 的 hits 进入 SQL 生成智能体和业务理解智能体。
2. SQL 生成智能体输出 candidates，每条候选带 assumptions、divergence_points、evidence。
3. 候选 SQL 被 `sql_probe` 执行，生成 query/explain 结果。
4. 业务理解结果和 probe 结果进入结构审查智能体。
5. probe 结果进入数据验证智能体。
6. 所有智能体输出和工具结果进入裁决智能体。
7. 最终 `MultiAgentRunResult.raw_output` 保存 agent_outputs 和 tool_results。

这样 benchmark 失败时，可以回看：

- 候选 SQL 是怎么来的；
- 哪些分歧点被探索了；
- 哪个智能体支持/反对某个候选；
- SQL 执行证据是什么；
- 裁决为什么选择最终 SQL。

## 8. 与现有 ChatBI 流程的关系

### 普通 query 链路

普通聊天问数当前仍走：

```text
backend/app/services/system/chatbi/query
```

多智能体没有插入普通 query 链路。

### DIN-SQL 链路

DIN-SQL 当前在：

```text
backend/app/services/system/chatbi/dinsql
```

多智能体与 DIN-SQL 并列，互不依赖。

### benchmark 链路

benchmark 的方法类型已扩展：

```text
LUOSHU_CHATBI
DIN_SQL
MULTI_AGENT
```

当 `method_type == MULTI_AGENT` 时，benchmark service 会调用 `MultiAgentSqlRunner`，并把最终 SQL 写入 benchmark case result。

## 9. 第一版覆盖的分歧点

当前 prompt 明确覆盖这些问题：

- 业务口径；
- 去重口径；
- 是否需要计算字段；
- JOIN 类型；
- 日期语义歧义；
- 单元格值匹配；
- 隐式条件；
- 分组维度选择；
- DISTINCT 必要性；
- NULL 处理。

这些分歧点并没有被建模成独立决策单元，而是通过“候选 SQL 的 divergence_points + 多智能体举证”来处理。

## 10. 一个例子

用户问题：

```text
统计每个地区下单客户数，按客户数降序取前 10
```

SQL 生成智能体可能生成：

- `COUNT(customer_id)` 版本；
- `COUNT(DISTINCT customer_id)` 版本；
- `INNER JOIN` 版本；
- `LEFT JOIN` 版本。

业务理解智能体会指出：

- “客户数”通常是实体数，不是订单行数；
- 如果一个客户有多笔订单，应使用 `COUNT(DISTINCT customer_id)`；
- “每个地区”是否保留没有订单的地区存在不确定性。

结构审查智能体会检查：

- JOIN 路径是否符合外键；
- GROUP BY 是否按地区 key；
- ORDER BY 是否和 LIMIT 配套。

数据验证智能体会比较：

- DISTINCT 前后计数是否不同；
- INNER/LEFT JOIN 行数是否不同；
- 空地区是否存在。

裁决智能体最后选择更符合问题和证据的 SQL。

## 11. 当前限制

第一版为了尽快可用，有几个限制：

1. 工具调用是 Runner 固定编排，不是 LLM 自主 function calling。
2. `value_founding` 已实现为工具，但当前 Runner 还没有让智能体动态调用它；后续可在业务理解或结构审查阶段按需触发。
3. `sql_probe` 当前只做 query/explain，没有专门的 DISTINCT 差异探针、NULL 分布探针、LEFT JOIN 空侧探针。
4. 候选 SQL 还没有做 AST 聚类，候选重复时会浪费 probe 成本。
5. 知识库当前面向 BIRD-DEV 的 `database_description`，不是通用业务知识库。
6. 裁决智能体可以修正 SQL，但第一版没有再对修正后的 SQL 做二次 probe。

## 12. 后续演进建议

优先级较高：

1. 给 `knowledge_search`、`sql_probe`、`value_founding` 暴露 function-calling schema。
2. 让业务理解智能体在发现字面值风险时主动调用 `value_founding`。
3. 对候选 SQL 做 AST 聚类，只 probe 每类代表 SQL。
4. 增加差异探针：
   - DISTINCT 前后计数；
   - LEFT JOIN 空侧数量；
   - NULL 值分布；
   - 日期边界命中数量。
5. 裁决输出 final_sql 后再跑一次 `sql_probe`，失败则回退到候选 SQL 或进入修复。
6. 在 benchmark detail 中更清晰展示 agent_outputs、tool_results 和 divergence_points。
